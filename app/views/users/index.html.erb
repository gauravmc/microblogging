<%= provide :title, 'All users' %>
<table class="table-condensed profile">
  <tr>
    <td class="main">
			<h1>All users</h1>
			<%= will_paginate %>

			<ul class="users">
				<%= render @users %>
			</ul>

			<%= will_paginate %>
    </td>
  </tr>
</table>